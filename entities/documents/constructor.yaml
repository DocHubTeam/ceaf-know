# Презентация для создания объекта документа
entities:
  docs:
    presentations:
      blank:
        title: Представление документа
        type: forms
        source: >
          (
            {
              "schema": {
                "type": "object",
                "properties": {
                  "location": {
                    "type": "string",
                    "format": "jsonata-query"
                  }
                }
              }
            }
          )
